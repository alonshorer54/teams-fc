-- ============================================================
--  עדכון קטן: הוספת עמודת הגדרות
--  מסנכרן את סדר העדיפויות של ההגרלה בין המחשב לטלפון.
--
--  הדביקו ב-SQL Editor של Supabase ולחצו Run.
--  בטוח להריץ גם אם כבר הרצתם — לא ימחק כלום.
-- ============================================================

alter table public.kohot_data
  add column if not exists settings jsonb not null default '{}'::jsonb;
