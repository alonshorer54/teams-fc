# Teams FC — סיכום הפרויקט

מסמך לימוד: מה בנינו, איך זה עובד, ולמה בחרנו כל דבר.
נכתב כדי שתוכל להסביר את הפרויקט לעומק — בריאיון עבודה, לחבר, או לעצמך בעוד חצי שנה.

**האתר:** https://alonshorer54.github.io/teams-fc/
**הקוד:** https://github.com/alonshorer54/teams-fc

---

## 1. מה זה בכלל

אפליקציית ווב לניהול משחק הכדורגל השבועי. הבעיה שהיא פותרת: כל שבוע צריך לחלק ~21 שחקנים
לשלוש קבוצות מאוזנות, ותמיד יש ויכוחים — מי עם מי, למה הקבוצה הזו חזקה יותר, ומי עוד לא שילם.

**מה האפליקציה עושה:**
- מנהלת מאגר שחקנים עם דירוג, חברויות, העדפות ותגיות
- מחלקת לשלוש קבוצות מאוזנות לפי סדר עדיפויות שהמשתמש קובע
- מאפשרת עריכה ידנית, ומתריעה מה נשבר כתוצאה ממנה
- שולחת את הכוחות לוואטסאפ כתמונה או כטקסט
- שומרת היסטוריה, לומדת מהתוצאות, ומזהה מגמות
- עוקבת אחרי גבייה שבועית

**בקנה מידה:** 7,683 שורות קוד ב־38 קבצים, 24 גרסאות שהועלו לאוויר.

---

## 2. הטכנולוגיות ולמה דווקא הן

| טכנולוגיה | תפקיד | למה נבחרה |
| --- | --- | --- |
| **TypeScript** | שפת הפיתוח | טיפוסים תופסים שגיאות בזמן כתיבה. בפרויקט הזה זה הציל אותנו כמה פעמים בשינויי מודל נתונים |
| **React 19** | ממשק המשתמש | מודל הצהרתי: מתארים איך המסך צריך להיראות, ו-React מעדכן לבד |
| **Tailwind CSS 4** | עיצוב | עיצוב ישירות ב-HTML. תמיכה טובה ב-RTL ובעיצוב רספונסיבי |
| **Vite 8** | כלי בנייה | פיתוח מהיר עם עדכון חי, ובנייה לקובץ קטן ומוכן לפרודקשן |
| **oxlint** | בדיקת קוד | מהיר מאוד, תפס באגים אמיתיים (למשל מפתחות כפולים ב-React) |
| **Supabase** | ענן | מסד נתונים + חשבונות + סנכרון בזמן אמת, בלי לכתוב שרת |
| **GitHub Pages** | אחסון | חינם, יציב, ומחובר ישירות ל-Git |
| **GitHub Actions** | פריסה | בונה ומעלה אוטומטית בכל דחיפה |

### ההחלטה הארכיטקטונית המרכזית: בלי שרת משלנו

האפליקציה היא **קבצים סטטיים בלבד** (HTML/JS/CSS) שרצים בדפדפן ומדברים ישירות מול Supabase.

**היתרונות:** אין שרת לתחזק, אין מה שיקרוס, עלות אפס, ופריסה בשניות.
**המחיר:** כל הלוגיקה גלויה בצד הלקוח, ולכן האבטחה חייבת להיאכף במסד הנתונים עצמו — לא בקוד.

זו נקודה טובה לריאיון: **מתי ארכיטקטורה כזו מתאימה ומתי לא.** היא מתאימה כאן כי אין לוגיקה
סודית ואין חישוב כבד בצד שרת. היא לא הייתה מתאימה אילו היינו צריכים, למשל, לחשב תשלומים
או להסתיר אלגוריתם קנייני.

---

## 3. איך הענן עובד

### מודל הנתונים

טבלה אחת, `kohot_data`, עם שורה אחת לכל משתמש:

```sql
user_id    uuid primary key references auth.users
players    jsonb   -- מערך השחקנים
history    jsonb   -- מערך ההגרלות
settings   jsonb   -- עדיפויות, מחזור נוכחי, גבייה
updated_at timestamptz
```

**למה JSONB ולא טבלאות רגילות?** זה "מודל מסמך" — כל הנתונים של משתמש בשורה אחת.
היתרון התגלה בפועל: הוספנו במהלך הפיתוח תגיות, מנהל קבוצה, העדפות אישיות, מקומות בטבלה
ומחזור מסונכרן — **בלי אף שינוי סכמה אחד**. במודל רלציוני כל תוספת כזו הייתה דורשת מיגרציה.

**המחיר:** אי אפשר לשאול שאילתות חכמות ("כל השחקנים בדירוג מעל 4 אצל כל המשתמשים").
לשימוש הזה זה לא נדרש — תמיד טוענים את כל הנתונים של משתמש אחד.

### האבטחה — Row Level Security

```sql
create policy "read own data" on kohot_data for select
  using (auth.uid() = user_id);
```

זה **חוק במסד הנתונים**, לא בדיקה בקוד. גם מי שייקח את המפתח מהקוד ויתחבר ישירות למסד לא
יקבל שורות של אחרים. אימתנו את זה בפועל: קריאה ללא התחברות מחזירה מערך ריק.

**נקודה חשובה להסבר:** המפתח (`anon key`) שמוטמע בקוד הוא **ציבורי בכוונה**. הוא רק מזהה
את הפרויקט. מה שמגן על הנתונים זה ה-RLS יחד עם הטוקן האישי שמתקבל בהתחברות.

### הסנכרון בין מכשירים

שלושה מנגנונים שעובדים יחד:

1. **טעינה** — בכניסה נטענים הנתונים מהענן ומחליפים את המטמון המקומי
2. **שמירה** — כל שינוי נשמר אחרי 900ms של שקט (*debounce*), עם השוואת "טביעת אצבע"
   (JSON מסודר) שמונעת כתיבה כשלא השתנה כלום
3. **דחיפה** — ערוץ Realtime מודיע על שינוי ממכשיר אחר ומעדכן את המסך מיידית

**הבעיה שהמנגנון הזה פותר:** בלי טביעת האצבע, כל שמירה שלנו הייתה חוזרת אלינו דרך ה-Realtime
וגורמת לשמירה נוספת — לולאה אינסופית. ההשוואה מזהה שההודעה היא ההד שלנו ומתעלמת ממנה.

### התאוששות מכשל

כשהוספנו את עמודת `settings` לטבלה שכבר הייתה בשימוש, נוצר סיכון: אם הקוד ינסה לכתוב עמודה
שלא קיימת, **כל השמירה תיכשל** — כולל השחקנים וההיסטוריה. זו הייתה נסיגה חמורה.

הפתרון: הקוד **בודק קודם** אם העמודה קיימת (שאילתה קטנה), ורק אז שולח אליה. אם היא חסרה,
ההגדרות נשארות מקומיות והשאר ממשיך לעבוד. זו דוגמה טובה ל*graceful degradation*.

---

## 4. האלגוריתם — הלב של הפרויקט

זו הבעיה המעניינת ביותר בפרויקט, ושווה להכיר אותה לעומק.

### הבעיה

לחלק N שחקנים ל־3 קבוצות כך שיתקיימו במקביל כמה תנאים סותרים: דירוג מאוזן, חברים יחד,
העדפות אישיות מכובדות, ותגיות מפוזרות. זו וריאציה על בעיית חלוקה (*partition problem*)
שהיא **NP-קשה** — אין אלגוריתם יעיל שמוצא את הפתרון המושלם.

### הפתרון: היוריסטיקה בשני שלבים

**שלב א' — בנייה חמדנית עם רעש**
מקבצים חברים לאשכולות (Union-Find), ממיינים מהגדול לקטן, ומשבצים כל אשכול לקבוצה עם הממוצע
הצפוי הנמוך ביותר. ב־25% מהמקרים בוחרים דווקא באפשרות השנייה הטובה — **הרעש הזה הוא מה
שגורם לכל הגרלה לצאת שונה.**

**שלב ב' — חיפוש מקומי**
עוברים על **כל** ההחלפות האפשריות בין זוגות שחקנים מקבוצות שונות, ומבצעים כל החלפה שמשפרת
את הציון. חוזרים עד שאין שיפור (*hill climbing*).

**ריסטארטים:** כל התהליך רץ 60 פעם, ונבחרת התוצאה הטובה ביותר. זה מתגבר על החיסרון המרכזי
של חיפוש מקומי — היתקעות במקסימום מקומי.

### פונקציית העלות

חמישה קריטריונים, כל אחד מחזיר קנס מנורמל ל־0..1:

| קריטריון | מה נמדד |
| --- | --- |
| דירוג | הפער בין הממוצע הגבוה לנמוך |
| חברויות | כמה זוגות חברים פוצלו |
| כימיה משחקית | פיזור האפקטים הנלמדים בין הקבוצות |
| מעדיף עם/בלי | כמה העדפות הופרו |
| תגיות | עד כמה בעלי אותה תגית מפוזרים שווה |

**החידוש: המשתמש קובע את הסדר.** המשקל של דרגה *i* הוא `1000 / 6^i` — כלומר כל דרגה שווה
פי 6 מזו שמתחתיה. זה מספיק כדי שהעליונה תכריע כמעט תמיד, אבל לא כל כך הרבה שהתחתונות
יהפכו לחסרות משמעות — הן עדיין שוברות שוויון בין חלוקות שקולות.

הוכחנו שזה עובד: העלאת "תגיות" מהמקום החמישי לראשון הקפיצה את הציון שלהן מ־78 ל־100,
במחיר של חברויות (86→57).

מעל הכל יש קנס ענק (5000) על גודל קבוצה חורג, כך שחלוקה לא־חוקית אף פעם לא תיבחר.

### הכימיה הנלמדת — ההחלטה הכי מעניינת

האפליקציה לומדת מההיסטוריה אילו זוגות מנצחים **יותר מהצפוי**: משווים את אחוז ההצלחה של הזוג
יחד לממוצע הביצועים האישיים שלהם. ההפרש הוא ה"אפקט".

**כאן הייתה החלטה עיצובית אמיתית:** מה לעשות עם המידע הזה?
- **אפשרות א':** לקבץ זוגות חזקים יחד → יוצר קבוצה חזקה במיוחד
- **אפשרות ב':** להתייחס לאפקט כחלק מחוזק הקבוצה ולקזז אותו

בחרנו ב־**ב'**, כי כל מטרת האפליקציה היא איזון. קיבוץ היה משיג בדיוק את ההפך ממה שהמשתמש רוצה.

**מגבלה שגילינו וכדאי להכיר:** שני שחקנים שמשוחקים תמיד יחד לא יופיעו בניתוח — הביצועים
האישיים שלהם *הם* ביצועי הזוג, אז ההפרש יוצא אפס. זו בעיה אמיתית של *confounding* בניתוח
נתונים, והדרך היחידה לפתור אותה היא שהזוג ייפרד לפעמים.

---

## 5. באגים שנתקלנו בהם — סיפורים טובים לריאיון

### המסך הלבן

אחרי שהרחבנו את מודל השחקן (מ־`friendOf` יחיד ל־`friendIds` מרובה), עריכת שחקן הפילה את
כל האפליקציה למסך ריק.

**הסיבה:** בנינו המרה של הנתונים הישנים **לתצוגה**, וזה עבד. אבל מסלול ה**שמירה** קיבל את
הנתונים הגולמיים, לפני ההמרה. הקוד ניסה לקרוא `p.friendIds.includes(...)` על שחקן שאין לו
את השדה → `undefined.includes` → קריסה.

**למה לא תפסנו את זה מוקדם:** בבדיקות זרענו נתונים בפורמט **החדש**, אז המסלול השבור לא נגע
בהם. הבדיקה לא שיקפה את המציאות. ברגע שזרענו את הפורמט הישן — נפל מיד.

**הלקח:** בדיקות מיגרציה חייבות להשתמש בנתונים הישנים האמיתיים.

**התיקון:** ההמרה רצה עכשיו משני צדי העדכון. בנוסף הוספנו **Error Boundary** — רשת ביטחון
שתופסת קריסה ומציגה הודעה מסודרת עם כפתור רענון, במקום דף לבן.

### המפתחות הכפולים

הקונסולה התמלאה באזהרות `Encountered two children with the same key`. השתמשנו בתאריך המשחק
כמפתח בטבלת הנוכחות — וברגע ששתי הגרלות נשמרו באותו יום, React השמיט תא.

**איך אימתנו את התיקון:** לא הסתמכנו על הודעות הקונסול (שנשארות במאגר גם אחרי תיקון), אלא
בדקנו **מבנית** — 10 הגרלות עם תאריכים כפולים צריכות לייצר 10 תאים בכל שורה. לפני התיקון
היו 9.

### סכומים מול ממוצעים

המשתמש ביקש לראות את **סכום** הדירוגים ולא ממוצע. אבל עם 20 שחקנים החלוקה היא 7/7/6,
והקבוצה בת השישה תמיד תצבור פחות — אז המדד הראה "איזון בינוני · פער 3.5" כשהקבוצות היו
מאוזנות לחלוטין.

**התיקון:** כשהגדלים שווים מוצג פער הסכומים; כשהם שונים מוצג פער ממוצע לשחקן, עם הבהרה
מפורשת שהסכומים אינם ברי-השוואה.

**הלקח:** מדד נכון מתמטית יכול להיות מטעה בהצגה.

### באגים נוספים

- **Tailwind 4** לא מאפשר `@apply` על מחלקות מותאמות — נדרש להגדיר את הבסיס על כל הווריאנטים יחד
- **PowerShell** הוסיף BOM ל־`package.json` והבנייה נשברה; PowerShell 5.1 גם משבש עברית ב־UTF-8
- **גלישה בטלפון** — הלשונית החמישית הקפיצה את רוחב הניווט מעבר למסך; תוקן בהסתרת האייקונים
- **המחזור לא הסתנכרן** — נשמר מקומית בכוונה, ובפועל התברר שזו הייתה החלטה שגויה מבחינת המשתמש

---

## 6. החלטות שכדאי להסביר

### מה שבנינו אחרת ממה שהתבקש

**ההעדפה על פני "כימיה"** — כשהמשתמש ביקש להוסיף "אהבה/שנאה" בנוסף ל"חבר של", זיהינו שאותו
זוג יכול להיות מסומן בשניהם ואז לקבל **משקל כפול** משני קריטריונים נפרדים — עיוות שלא התכוון
אליו. הפתרון: חברים לא מופיעים ברשימות ההעדפה בכלל, והנתונים הקיימים מנוקים אוטומטית.

**דירוג מקומות במקום מנצח יחיד** — המשתמש ביקש "שתי קבוצות ניצחו ואחת הפסידה". במקום
להוסיף עוד אפשרות לרשימה, החלפנו את המודל כולו לדירוג 1-2-3 עם שוויון מותר, שמכסה את כל
התרחישים. הנתונים הישנים ("לבן ניצחה") מומרים אוטומטית.

### מה שסירבנו לבנות ולמה

**חיבור לוואטסאפ** — אין דרך לגיטימית. ה-API הרשמי לא ניגש לקבוצות או לסקרים, והספריות
הלא-רשמיות מפרות את תנאי השימוש ומסכנות את מספר הטלפון. **החלופה שבנינו:** הדבקת רשימה
עם זיהוי שמות חכם — הקבוצה ממשיכה עם הסקר כרגיל, והמנהל מדביק פעם אחת.

**חיבור לביט** — אין ממשק ציבורי שמאפשר לאפליקציה חיצונית לראות העברות. זה חסום מטעמי
רגולציה אצל כל הארנקים הישראליים. **מה שבנינו במקום:** סימון ידני בקליק עם ארבע שיטות תשלום,
מעקב התקדמות, ותזכורת מוכנה לשליחה.

**גרירה בטלפון** — HTML5 drag-and-drop לא עובד במסך מגע. במקום להילחם בזה, הדרך החלופית
(נגיעה בשני שחקנים) עובדת מצוין, וההסבר על גרירה מוצג רק במחשב.

זו נקודה חזקה לריאיון: **לדעת מתי לא לבנות משהו, ולהציע חלופה שפותרת את הצורך האמיתי.**

---

## 7. תכונות מעניינות מבחינה טכנית

### יצירת תמונה לשיתוף

במקום שהמשתמש יצלם מסך, האפליקציה **מציירת** את הכוחות על `<canvas>` בגודל 2160×1408
ופותחת את תפריט השיתוף של המכשיר דרך **Web Share API**. משם בוחרים וואטסאפ והתמונה נשלחת.
במחשב, שבו אין תפריט שיתוף, הכפתור יורד לגיבוי של הורדת קובץ.

### PWA — התקנה כאפליקציה

שלושה חלקים: `manifest.webmanifest` (שם, אייקונים, מסך מלא), **Service Worker** שמאפשר
עבודה בלי רשת, וזיהוי `beforeinstallprompt` לדיאלוג התקנה. ב-iOS אין API כזה, אז שם מוצגות
הוראות ידניות.

**האייקונים נוצרו בקוד** — סקריפט Node שכותב PNG מאפס (מקודד chunks ו-CRC32 ידנית), בלי
שום ספרייה גרפית.

**ניהול גרסאות המטמון:** סקריפט שרץ אחרי הבנייה מטביע מזהה גרסה ב-Service Worker, כך שכל
גרסה מקבלת מטמון משלה והישן נמחק אוטומטית.

### זיהוי שמות מטקסט חופשי

הפרסר מנקה ניקוד, אמוג'י, מספור וסימני רשימה, ואז מנסה התאמה מדויקת → שם פרטי → הכלה.
שם שמתאים לכמה שחקנים מוצג להכרעה ידנית. בבדיקה זיהה 9 מתוך 10 שמות מרשימה מבולגנת בכוונה.

### דוח שינויים

אחרי כל עריכה ידנית, האפליקציה משווה את המצב לפני ואחרי ומסבירה בשפה טבעית מה נשבר:
חברים שהופרדו, העדפות שהופרו, פער דירוג שגדל, קבוצות בגדלים שונים, ריכוז תגיות. כולל ביטול
בלחיצה.

---

## 8. איך לספר את זה בריאיון

**המשפט הפותח:**
> "בניתי אפליקציית ווב לניהול משחק הכדורגל השבועי שלי. הליבה שלה היא אלגוריתם אופטימיזציה
> שמחלק שחקנים לקבוצות מאוזנות לפי כמה קריטריונים סותרים, כשהמשתמש קובע את סדר החשיבות."

**שאלות שסביר שיישאלו, ומה לענות:**

*"למה לא השתמשת ב-Redux / Next.js / ספריות נוספות?"*
כי לא היה צורך. ניהול המצב נעשה עם React בלבד, והוספת ספרייה הייתה מוסיפה תלות ומורכבות
בלי תועלת. הכלל שהנחה: לא להוסיף כלי בלי בעיה שהוא פותר.

*"איך אתה מבטיח שמשתמש אחד לא יראה נתונים של אחר?"*
Row Level Security ברמת מסד הנתונים, לא ברמת הקוד. גם מי שייקח את המפתח הציבורי מהקוד לא
יקבל שורות של אחרים. בדקתי את זה בפועל.

*"האלגוריתם מוצא את הפתרון האופטימלי?"*
לא, וזו החלטה מודעת — הבעיה NP-קשה. זו היוריסטיקה של בנייה חמדנית + חיפוש מקומי עם 60
ריסטארטים. בפועל היא מגיעה לפער 0.00 בין הקבוצות תוך פחות משנייה, וזה מספיק טוב.

*"מה היה הקשה ביותר?"*
מיגרציות של מודל הנתונים בזמן שהמערכת כבר בשימוש עם נתונים אמיתיים בענן. הוספת שדות
שמחייבת שהנתונים הישנים ימשיכו לעבוד — ובאג אחד כזה הפיל לי את כל האפליקציה.

*"איך בדקת?"*
בדיקה ידנית מקיפה בדפדפן, כולל אמולציית מגע ומסך טלפון. לא כתבתי בדיקות אוטומטיות — זו
פשרה מודעת של פרויקט אישי, ובמערכת שמשרתת יותר משתמשים הייתי מוסיף אותן.

**מה שמראה בגרות הנדסית — הדגש על זה:**
- ידיעה מתי **לא** לבנות (ביט, וואטסאפ) ולהציע חלופה שפותרת את הצורך
- טיפול בכשלים בלי לשבור מה שעובד (זיהוי עמודה חסרה, Error Boundary)
- תיקון מדדים מטעים גם כשהם נכונים מתמטית
- ניקוי כפילויות במודל הנתונים במקום להוסיף עוד שדה

---

## 9. מה היה חסר בפרויקט אמיתי

היושר כאן חשוב יותר מלהיראות טוב:

- **אין בדיקות אוטומטיות** — הכול נבדק ידנית
- **אין ניטור** — אם משתמש נתקל בשגיאה, אין לי דרך לדעת
- **חשבון אחד = קבוצה אחת** — אין תמיכה בכמה מנהלים לאותה קבוצה
- **אין הגבלת קצב** — משתמש זדוני יכול להציף את המסד בבקשות
- **קוד הליבה גדול** — `DrawView` צמח מעבר למה שנוח לתחזוקה, ובפרויקט ארוך-טווח הייתי מפצל

הפרויקט נבנה כפתרון לבעיה אישית אמיתית ולא כתרגיל, וזה משפיע על מה שהושקע ובמה לא.
